import geometry.*;
import math.*;
import shader.*;

class App {
    public static void main(String[] args){
        // new Scene("./scenes/myScene.json").makeImage(new NormalShader(), "gson");
    }
}